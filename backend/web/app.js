const rows=document.querySelector('#rows'),message=document.querySelector('#message');async function load(){const r=await fetch('/api/v1/alerts');const d=await r.json();rows.innerHTML=d.items.map(x=>`<tr><td>${x.id}</td><td>${x.region}</td><td>${x.severity}</td><td>${x.status}</td><td>${x.crew_assigned}</td><td><button data-id="${x.id}">标记已控制</button></td></tr>`).join('');rows.querySelectorAll('button').forEach(b=>b.onclick=()=>update(b.dataset.id))}async function update(id){const r=await fetch(`/api/v1/alerts/${id}/status`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'contained'})});message.textContent=r.ok?'火情状态已更新':'更新失败';if(r.ok)load()}document.querySelector('#refresh').onclick=load;load().catch(()=>message.textContent='无法读取火情记录');
